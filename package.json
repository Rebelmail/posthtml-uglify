{
  "name": "posthtml-uglify",
  "version": "1.2.0",
  "description": "A PostHTML plugin to rewrite CSS identifiers in HTML",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "coveralls": "jest --coverage && cat ./coverage/lcov.info | coveralls",
    "bench": "node ./scripts/benchmark.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Rebelmail/posthtml-uglify.git"
  },
  "keywords": [
    "posthtml",
    "posthtml-plugin",
    "html",
    "uglify",
    "compress",
    "minify"
  ],
  "author": "Rebel",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/Rebelmail/posthtml-uglify/issues"
  },
  "homepage": "https://github.com/Rebelmail/posthtml-uglify",
  "dependencies": {
    "css-class-generator": "github:Rebelmail/css-class-generator#only-lowercase",
    "postcss-safe-parser": "^3.0.1",
    "postcss-selector-parser": "^3.1.1",
    "posthtml": "^0.9.2",
    "posthtml-parser": "^0.2.1",
    "posthtml-render": "^1.0.6"
  },
  "devDependencies": {
    "benchmark": "^2.1.4",
    "coveralls": "^3.0.0",
    "eslint": "^4.11.0",
    "eslint-config-airbnb-base": "^12.1.0",
    "eslint-plugin-import": "^2.8.0",
    "jest": "^21.2.1"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
